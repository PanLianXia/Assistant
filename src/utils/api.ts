// import { streamRequest } from "./request";

// const api = {
//     chatMessageStream : (data: any, onMessage: any, onError: any, onFinish: any) => {
//         const { inputs, difyConversationId, query } = data;
//         return streamRequest('/chat-messages',{ inputs, conversation_id: difyConversationId, query }, {onMessage, onError, onFinish})
//     }
// }

// export { api }
